"""
Sistema de Monitoramento e Métricas para LSTM API
==================================================

Este módulo fornece monitoramento abrangente para o modelo LSTM em produção,
incluindo métricas de performance, recursos do sistema e qualidade do modelo.

Estrutura do pacote:
- core: Componentes principais (métricas, monitoramento, sistema)
- drift: Monitoramento de data drift
- integrations: Integrações com sistemas externos
- utils: Utilitários e ferramentas auxiliares
"""

# Importar componentes principais
from src.monitoring.core.metrics import PredictionMetrics, SystemMetrics
from src.monitoring.core.monitor import ModelPerformanceMonitor
from src.monitoring.core.system import SystemMonitor

# Importar utilitários
from src.monitoring.utils.decorators import (
    time_execution,
    monitor_prediction, 
    track_prediction
)

# Importar integrações
from src.monitoring.integrations.dashboard import setup_monitoring_dashboard
from src.monitoring.integrations.prometheus import (
    setup_prometheus_metrics,
    start_prometheus_server,
    update_model_info
)

# Importar monitoramento de drift
from src.monitoring.drift.monitor import DriftMonitor
from src.monitoring.drift.detector import DriftDetector
from src.monitoring.drift.collector import DataCollector
from src.monitoring.drift.report import ReportGenerator

# Criar instância global do monitor para compatibilidade com código existente
performance_monitor = ModelPerformanceMonitor(model_version="1.0.0", log_file="monitoring.log")

# Manter uma referência para os módulos de monitoramento
__all__ = [
    # Core
    'PredictionMetrics',
    'SystemMetrics',
    'ModelPerformanceMonitor',
    'SystemMonitor',
    
    # Utils
    'time_execution',
    'monitor_prediction',
    'track_prediction',
    
    # Integrations
    'setup_monitoring_dashboard',
    'setup_prometheus_metrics',
    'start_prometheus_server',
    'update_model_info',
    
    # Drift
    'DriftMonitor',
    'DriftDetector',
    'DataCollector',
    'ReportGenerator',
    
    # Global instance
    'performance_monitor'
]

# Fim da configuração do módulo de monitoramento
